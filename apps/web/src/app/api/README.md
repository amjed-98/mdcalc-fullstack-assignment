# Route handlers (reserved)

This folder is intentionally empty. Next.js route handlers are **not**
required for the assignment — the web app should call the Express API at
`NEXT_PUBLIC_API_BASE_URL`. Add files here only if you have a clear reason
(e.g. a BFF shim) and explain it in `NOTES.md`.
