# Terraform (stub)

This folder is a placeholder for the production IaC. It is **not** part of
the take-home scope — do not modify files here unless the assignment asks
you to.

In the real repo, this directory defines:

- VPC + subnets + NAT
- RDS Postgres
- ECS Fargate services for `api` and `web`
- CloudFront + S3 for static assets
- CloudWatch alarms and dashboards
